# Goal: pipeline reaches test stage but fails due to an intentionally failing test.
pipeline:
  name: java-test-failure

stages:
  - build
  - test

compile:
  stage: build
  script: scripts/compile.sh

failing-tests:
  stage: test
  needs: [compile]
  script: scripts/test-failing.sh